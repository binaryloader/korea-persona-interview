# 직접 의존성 진입점.
#
# v1.1.0부터 정본 dependencies는 ``pyproject.toml``의 ``[project.dependencies]``
# 섹션이다(dependency.md §2). 본 파일은 ``pip install -r requirements.txt``
# 흐름에 친숙한 사용자를 위한 호환성 진입점이며 PEP 508 ``-e`` 셀프 설치로
# pyproject 의존성을 그대로 끌어오게 한다. 트랜지티브 포함 전체 트리 핀은
# ``requirements.lock``에 동결되어 있고, 프로덕션 배포는 lock으로 설치한다.
-e .
